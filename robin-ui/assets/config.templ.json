{
  "app": {
    "APP_VERSION": "${APP_VERSION}",
    "ENV_NAME": "${ENV_NAME}"
  },
  "robinBackend": {
    "BASE_URL": "${ROBIN_BACKEND_URL}"
  },
  "robinNest": {
    "BASE_URL": "${ROBIN_NEST_URL}"
  }
}
